version: '3.8'

services:
  announcemntstest:
    build: .
    container_name: announcemntstest
    restart: unless-stopped
    environment:
      DB_HOST: host.docker.internal
      DB_PORT: 5432
      DB_USER: postgres
      DB_PASSWORD: 123
      DB_NAME: project_farm
    ports:
      - "8086:8086"